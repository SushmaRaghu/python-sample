name: "CodeQL Analysis"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 0 * * 1'

jobs:
  analyze:
    name: Analyze Code with CodeQL
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v2
      with:
        languages: 'python'  # Specify the language you're analyzing

    - name: Build project
      run: |
        # Add any necessary build steps here (if required)

    # Run CodeQL analysis with custom output path
    - name: Run CodeQL analysis
      uses: github/codeql-action/analyze@v2
      with:
        output: './codeql-results'  # Store intermediate analysis results here

    # Upload SARIF results (from the default SARIF file generated)
    - name: Upload SARIF results
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: './codeql-results/results.sarif'  # Reference the SARIF file generated by the analysis
        category: 'python-security'
